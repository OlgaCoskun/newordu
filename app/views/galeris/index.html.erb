

  <tbody>
    <% @galeris.each do |g| %>
      <tr>
        <td><%= g.title %></td>
        <td><%= image_tag(g.avatar.medium.url) if g.avatar? %></td>
        <td><%= link_to 'Show', g %></td>
        <td><%= link_to 'Edit', edit_galeri_path(g) %></td>
        <td><%= link_to 'Destroy', g, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New photo', new_galeri_path %>

<div class="container">
  <h2>Galeri</h2>
  <br>
  <% @galeris.each do |g| %>

    <div class="row">
      <div class="col-md-4">
         <%= image_tag(g.avatar.medium.url) if g.avatar? %>
          <%= g.title %>
        </div>

        <div class="col-md-4">
         <%= image_tag(g.avatar.medium.url) if g.avatar? %>
          <%= g.title %>
        </div>

        <div class="col-md-4">
         <%= image_tag(g.avatar.medium.url) if g.avatar? %>
          <%= g.title %>
        </div>
    </div>

  <% end %>
</div>



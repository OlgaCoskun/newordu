<h2>All photos</h2>

<%= link_to 'All photos', galeris_path %>
<h1><%= @galeri.title %></h1>

<%= image_tag(@galeri.image.url, alt: 'Image') if @galeri.image? %>




<%= link_to 'Eddddit', edit_galeri_path(@galeri) %> |
<%= link_to 'Back', galeris_path %>

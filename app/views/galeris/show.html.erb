<p>
  <strong>Title:</strong>
  <%= @galeri.title %>
</p>
<%= image_tag(@galeri.avatar.thumb.url) if @galeri.avatar? %>

<% if can? :update, Galeri %>
<%= link_to 'Edit', edit_galeri_path(@galeri), class: "btn btn-warning btn-xs" %>
<% end %>

<%= link_to 'Back', galeris_path, class: "btn btn-primary btn-xs" %>

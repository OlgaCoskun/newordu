<% provide(:title, 'Blog') %>
<% if @article.errors.any? %>
<h2><%= pluralize(@article.errors.count, "error") %> prohibited this post from being saved:</h2>
<ul>
    <% @article.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
</ul>
<% end %>

<%= form_for(@article) do |f| %>

    <p>
        <%= f.label :title, "Duyurular" %><br>
        <%= f.text_field :title %>
    </p>

    <p>
        <%= f.label :description, "Aciklamalar" %><br>
        <%= f.text_area :description %>
    </p>

    <p>
        <%= f.submit class: "btn btn-primary", type: "submit", value: "Tamam", data: { disable_with: "Yükleniyor..."} %>
    </p>

<% end %>

<%= link_to "Dönmek hepsi blog sayfa", articles_path %>

<script>
  $( function() {
    $( "#datepicker" ).datepicker();
  } );
  </script>

<% if @duyurular.errors.any? %>
<h2><%= pluralize(@duyurular.errors.count, "error") %> prohibited this post from being saved:</h2>
<ul>
    <% @duyurular.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
</ul>
<% end %>

<%= form_for :duyurular, url: duyurulars_path do |f| %>

    <p>
        <%= f.label :title, "Duyurular" %><br>
        <%= f.text_field :title, id:"datepicker" %>
    </p>

    <p>
        <%= f.label :text, "Aciklamalar" %><br>
        <%= f.text_area :text, id: "datepicker" %>
    </p>

    <p>
        <%= f.submit class: "btn btn-primary", type: "submit", value: "Tamam", data: { disable_with: "Yükleniyor..."} %>
    </p>

<% end %>

<%= link_to "Dönmek hepsi blog sayfa", duyurulars_path %>
